---
exclude_paths:
  - .github/
  - venv/
  - packer/
  - terraform/
  - vagrant

# Enable offline mode for CI environments
offline: true

# Skip dependency checks
skip_list:
  - syntax-check[specific]
  - syntax-check[unknown-module]
