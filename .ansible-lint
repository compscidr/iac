---
exclude_paths:
  - .github/
  - venv/
  - packer/
  - terraform/
  - vagrant

# Enable offline mode for CI environments
offline: true

# Set warnings for issues related to missing modules
warn_list:
  - fqcn[unknown-module]
  - var-naming[no-role-prefix]
